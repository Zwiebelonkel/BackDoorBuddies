# Blender room sources

Export room visuals as `.glb` into this directory. Keep the corresponding
procedural Godot wrapper scene in `res://procedural/rooms/` and follow
`res://procedural/rooms/BLENDER_ROOM_WORKFLOW.md`.

Do not add a raw `.glb` directly to the generator's `room_scenes` array.
